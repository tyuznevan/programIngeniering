name: Python CI

on: [push, pull_request]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'
          
      - name: Install dependencies
        run: pip install -r requirements.txt
          
      - name: Run tests
        run: pytest -v tests/  # -v для подробного вывода

      - name: "🍿 Выполняем важный скрипт"
        run: |
          echo "📜 Инструкция: Как открыть банку кукурузы:"
          echo "1. Возьмите банку и консервный нож"
          echo "2. Подденьте крышку ножом"
          echo "3. Прокрутите нож по окружности банки"
          echo "4. 🎉 Поздравляем! Кукуруза освобождена!"
          echo "Текущая дата: $(date)"
